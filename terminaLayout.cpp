// Main file for getting current terminal window its position and size, then convert it into a layout that can be used to display the image of the terminal window in the correct position and size.
#include <string>
#include "layoutWindow.hpp"
#include "DataType.hpp"
#include "socketSend.hpp"
#include <unistd.h>


int main() {
    
    WindowData data = WindowData(0);

    return 0;
}
