zamn idk where to start

(-) . Makesure is the 'activewindows2' the real thing to read for the movement? i think other can qualify like openwndow and move window or something.
2. Use to detech all of window terminal inside a workspace not         if (!outputData2.empty() && outputData2.size() >= 2 && outputData2[0].size() >= 2 && outputData2[1].size() >= 2) {
            std::cout << "Position: " << outputData2[0][0] << ", " << outputData2[0][1] << std::endl;
            std::cout << "Size: " << outputData2[1][0] << ", " << outputData2[1][1] << std::endl;
        }the active openwndow
( V ) find how to show video / maybe image frist for mvp. Makesure it blazing fast and cached so its even faster.
(v). i could use PID way instead of the window address thingy
(?). check only terminal //// kitty can run this program
(v). Make the socketsend more moduular. which is to split everyshit in the worlllld
7. Performance improvement : cached the json window  thingy result data in the socketSend and like change the data if theres update in the current workspace, maybe use static local or someshit (search different implementation between static and global and local implementation type shiiii it sooo goood but like i think i should not do that in here yet)
(-). Use implementation of the old socketRecieve to get the PID and workspace id cuz the previous implementation doesnt work cuz its not a GUI program.
(v). Make dedicated file of the output form of the window thingy
(v). check if the one it open is terminal or not
(v). instead of few specializied function make it full type.'
(v). use the cartesian coordinate converter for the windows
(v). we can add feature to chcekc what terminal is it, like what emulator/shell, by recursively scan previous of hte tree until we got the terminal emulator scripting type shit.
13. Use the WorkspaceData as parameter to must of socket function
14. Finish the DataType.hpp for the refresh data function
(V). USE PID INSTEAD of tittle shit to find window ID
16. Implement the  correct naming of the SHM
17. in getImageSHM fix the vector return error thingy.
(V). Add implementation of custom Base64 for custum SHM name cuz im lazy rn
19. add implementation of the dynamic resolution video cuz im very lazy and hungry rn
(V). ADD delete of the SHM File of the consumer.cpp 
(V). fix SHM not deleting at exit
22. Use current cursor location, instead of top left, but maybe not
23. Add audio support from the producer
24. add video path modularity in DataType.hppp in theres siawfoijweeoi weeo
25. Add universal videodata, either in the consumer / or producer
26. what if the config isnt yet built
27. use std::runtime_error // clean debug // clean error logging
28. CHange /vp_static into /HyprLarp-Producer
29. COmbine the layout thingy
video :
1. use kitty, ghossty, WezTerm
2. SHM memory IPC shit
3. use mpv
4. what way should i use, i think i need to make a pointer to the video run, then use pointer and just do crop crop
5. How to do the layout one    configFileExists(const std::string &path)


How to make it like simultinuesly render? 
1. Decoder : 
2. 

What to find out?
- YUV
-  

How can i crop the video then?
mpv --vf=crop=width:height:x_offset:y_offset video.mp4

command mpv : 
mpv --vo=kitty --vo-kitty-use-shm=yes --osc=no --osd-level=0 --profile=sw-fast --really-quiet video.mp4


current implementation for like suitable shitthingy :
- instead of each running kity mpv, one decode and other refernce the memory
- propable use https://gitlab.com/sh4lt/sh4lt
- idk help me

dumping stuffs

Use:
x PID → identify your process
x window address → identify your exact window
- parent process tree → detect terminal launch 
- Hyprland IPC → detect workspace/window state
- mmap

File type:
- [position, size]
- tittle name
- workspace id


Summary of the deep research report from chatgptdih:
-  OKAY MY PROJECT IS LIKE 20% done , what the fuck is this


Things that i need to keep an i out :
1. Kitty rendering : Kitty default on using cursor for image location i think but thres alternative using : direct grid positioning, and absolute windows/placement
2. Kitty rendering : kitty normaly render at column x rows location, but we can get more precuse down to pixel by 
3. Producer checker : implement heartbeat to determine if producer is dead or not to tell client that it shoudl abort if producer dead cuz why
4. implementation consideration : NUMA device ? check point 12.4 on kitty video pipeline pdf
5. Cache / optimization : at point 12.2 see file cache behaviour improvement

Improvement:
1. in videodecoder.hpp in converter funciton, we can change the av image alloc last parameter to 32, but that recuire tweeking.
2. In terminalLayout.cpp in the main function, we can like reuse the jsondata with the producer main central socket thingy
3. When running the main executable, the first terminal is the one who be the producer and consumer, the rest will be just consumer
4. use Catch SIGWINCH
5. migrate all nlohmman/json into simdjson
6. use ondemand api instead of dom for simdjson in DataType.cpp or elsewhere
7. try to use try catch for freaky error

Component of the current program :
1. Consumer (consumer.cpp) : The one who render the video in terminal
2. Producer (producer.cpp) : The one who decode the video and determine a change in the terminal layout
3. Terninal (terminal.cpp) : The one who determine the internal dimention 
4.  

GUI
1. Add resize
2. Add data get
3. Save data

JSON format : from GUI to send to producer
{
    "videoPath" : "/sdf/sdf.mp4",
    "cornerLocation" : [
        [topleft], [topright], [bottomleft], [bottomrihgt]
    ]
}